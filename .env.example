# Reown (WalletConnect) Cloud project id — https://cloud.reown.com
# Required for wallet connect (injected + WalletConnect/mobile wallets) to work.
# Must be prefixed with VITE_ so Vite exposes it to client-side code.
VITE_REOWN_PROJECT_ID=
